services:
  deluge:
    image: linuxserver/deluge:latest
    # User configures Deluge here — expose daemon RPC port 58846.

  deluge-retain:
    build: .
    volumes:
      - ./deluge-retain.toml:/etc/deluge-retain.toml:ro
    command: ["--config", "/etc/deluge-retain.toml"]
    restart: unless-stopped
    depends_on:
      - deluge
